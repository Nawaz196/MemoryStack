import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  mainContainer: {
    display: "flex",
    alignItems: "center",
    gap: "15px 0px",
    "flex-wrap": "wrap",
  },
  smMargin: {
    // margin: theme.spacing(1),
  },
  actionDiv: {
    textAlign: "center",
  },
}));
